using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DuckGame.R6S
{
    /*[EditorGroup("Faecterr's|Stuff")]
    public class SimpleBarricade : Thing
    {
        //private SpriteMap _sprite;

        public SimpleBarricade(float xpos, float ypos) : base(xpos, ypos)
        {
            this.center = new Vec2(8f, 8f);
            //this._sprite = new SpriteMap(GetPath(".png"), 16, 16);
            //this.graphic = this._sprite;
            this._visibleInGame = false;
        }
        public override void Initialize()
        {
            for (int i = 0; i < 17; i++)
            {
                Wall wall = new Wall(this.position.x - 2, this.position.y + i - 8f);
                wall.health = 10f;
                wall.thickness = 1f;
                wall.col = Color.Orange;
                Level.Add(wall);
            }
            for (int i = 0; i < 17; i++)
            {
                Wall wall = new Wall(this.position.x + 2, this.position.y + i - 8f);
                wall.health = 10f;
                wall.thickness = 1f;
                wall.col = Color.Orange;
                Level.Add(wall);
            }
            for (int i = 0; i < 17; i++)
            {
                Wall wall = new Wall(this.position.x - 1, this.position.y + i - 8f);
                wall.health = 5f;
                wall.thickness = 2f;
                Level.Add(wall);
            }
            for (int i = 0; i < 17; i++)
            {
                Wall wall = new Wall(this.position.x + 1, this.position.y + i - 8f);
                wall.health = 5f;
                wall.thickness = 2f;
                Level.Add(wall);
            }
            base.Initialize();
        }
    }*/
}
